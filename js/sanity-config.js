/* Sanity CMS connection settings.
   After creating your Sanity project (see SANITY-SETUP.md), paste your
   Project ID below. Until then the site simply shows its built-in content. */
window.WGF_SANITY = {
  projectId: "50kkp3hz",
  dataset: "production"
};
